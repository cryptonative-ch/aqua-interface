name: Develop CI/CD

on:
  push:
    branches:
      - github_action
jobs:
  develop-ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install dependencies
        run: npm install

      - name: Run the tests and generate coverage report
        run: npm test -- --coverage

      - name: Build
        run: npm run build
  # develop-deploy:
  #   runs-on: ubuntu-latest
  #   needs: [develop-ci]
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: Deploy
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{secrets.DEV_NODE_IP}}
  #         username: ubuntu
  #         key: ${{ secrets.SSH_KEY }}
  #         port: 22
  #         script: |
  #           /bin/sh ./mesa-interface/develop_deploy.sh